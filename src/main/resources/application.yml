server:
  port: 9999
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 1234
    url: jdbc:mysql://localhost:3306/x?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&useSSL=true
  redis:
    port: 6379
    host: localhost
logging:
  level:
    com.lantu: debug
mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

